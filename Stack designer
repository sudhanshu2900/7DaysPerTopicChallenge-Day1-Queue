PROBLEM: n = 5
         arr = {1 2 3 4 5}
Output:
         5 4 3 2 1
         
         
SOLUTION:

stack<int>_push(int arr[],int n)         //It simply insert elements of array into stack
{
    stack<int> s;
    for(int i=0;i<n;i++)
        s.push(arr[i]);
    return s;
}

void _pop(stack<int> s)                 //It display all the element of stack
{
    while(s.size()>0){
        cout<<s.top()<<" ";
        s.pop();
    }
}






